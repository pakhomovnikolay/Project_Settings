<Window x:Class="Project_Settings.Views.Windows.ColorListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_Settings.Views.Windows"
        xmlns:vm="clr-namespace:Project_Settings.ViewModels"
        mc:Ignorable="d"
        Title="ColorListWindow" Height="150" Width="300" WindowStyle="None">
    <Window.DataContext>
        <vm:MainWindowsViewModel/>
    </Window.DataContext>
    <Grid>
        <ListBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" x:Name="ColorList" SelectionChanged="ColorList_SelectionChanged">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Rectangle Fill="{Binding Name}" OpacityMask="Black" Width="20" Height="20" StrokeThickness="1" Stroke="Black" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
